[project]
name = "mmllm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "karim", email = "karim.samir.lotfy@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
mmllm = "mmllm:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
